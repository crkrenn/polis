# Test: local docker

# Dev-cloud
* 23/08/27: new dev-cloud.env works with `make e2e-run`
* add .env link in Makefile; edit values
* cy create user wait for creating user to appear and disappear
*
* test email, other keys
* Run server locally
* Run server in cloud
* Run math locally
* Run math in cloud

# Prod-cloud

# Future

* <https://cloud.google.com/spanner/docs/pgadapter>
* fix postgres math error in dev-cloud.env
